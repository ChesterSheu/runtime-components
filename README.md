# Runtime Components

This repository contains my runtime components and configuration files for bash, zsh, vim, gdb, and git.

The install.sh script installs these configuration files into their appropriate locations.
